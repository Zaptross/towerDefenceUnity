using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{
    // Cameras
    [SerializeField] private Cam aerial;
    [SerializeField] private Cam action;
    [SerializeField] private Cam[] players_fp;
    [SerializeField] private Cam[] players_3p;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
